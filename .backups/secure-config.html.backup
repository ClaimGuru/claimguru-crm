<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ClaimGuru - Secure API Configuration</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .config-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            padding: 40px;
            max-width: 600px;
            width: 100%;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .header h1 {
            color: #333;
            font-size: 28px;
            margin-bottom: 10px;
        }
        
        .header p {
            color: #666;
            font-size: 16px;
        }
        
        .security-notice {
            background: #e8f5e8;
            border: 1px solid #4caf50;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 30px;
        }
        
        .security-notice h3 {
            color: #2e7d32;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .security-notice ul {
            color: #2e7d32;
            margin-left: 20px;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        .form-group label {
            display: block;
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
            font-size: 14px;
        }
        
        .form-group input {
            width: 100%;
            padding: 15px;
            border: 2px solid #e1e5e9;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s;
            font-family: 'Monaco', 'Courier New', monospace;
        }
        
        .form-group input:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        
        .form-group .helper-text {
            font-size: 12px;
            color: #666;
            margin-top: 5px;
        }
        
        .current-config {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 30px;
        }
        
        .current-config h3 {
            color: #333;
            margin-bottom: 15px;
        }
        
        .config-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid #e1e5e9;
        }
        
        .config-item:last-child {
            border-bottom: none;
        }
        
        .config-label {
            font-weight: 500;
            color: #333;
        }
        
        .config-status {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 500;
        }
        
        .config-status.present {
            background: #e8f5e8;
            color: #2e7d32;
        }
        
        .config-status.missing {
            background: #ffebee;
            color: #c62828;
        }
        
        .btn-group {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }
        
        .btn {
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            flex: 1;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }
        
        .btn-secondary {
            background: #f8f9fa;
            color: #333;
            border: 2px solid #e1e5e9;
        }
        
        .btn-secondary:hover {
            background: #e9ecef;
        }
        
        .progress {
            display: none;
            text-align: center;
            margin-top: 20px;
        }
        
        .spinner {
            border: 3px solid #f3f3f3;
            border-top: 3px solid #667eea;
            border-radius: 50%;
            width: 30px;
            height: 30px;
            animation: spin 1s linear infinite;
            margin: 0 auto 10px;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .result {
            margin-top: 20px;
            padding: 15px;
            border-radius: 8px;
            display: none;
        }
        
        .result.success {
            background: #e8f5e8;
            color: #2e7d32;
            border: 1px solid #4caf50;
        }
        
        .result.error {
            background: #ffebee;
            color: #c62828;
            border: 1px solid #f44336;
        }
        
        .toggle-visibility {
            margin-left: 10px;
            padding: 5px 10px;
            background: #f0f0f0;
            border: 1px solid #ddd;
            border-radius: 4px;
            cursor: pointer;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="config-container">
        <div class="header">
            <h1>🔐 Secure API Configuration</h1>
            <p>Add your missing API keys to enable full ClaimGuru functionality</p>
        </div>
        
        <div class="security-notice">
            <h3>🛡️ Security Features</h3>
            <ul>
                <li>Keys are processed locally in your browser</li>
                <li>No transmission over insecure connections</li>
                <li>Automatic validation before storage</li>
                <li>Encrypted configuration generation</li>
            </ul>
        </div>
        
        <div class="current-config">
            <h3>Current Configuration Status</h3>
            <div class="config-item">
                <span class="config-label">Supabase URL</span>
                <span class="config-status present">✓ Present</span>
            </div>
            <div class="config-item">
                <span class="config-label">Supabase Anon Key</span>
                <span class="config-status present">✓ Present</span>
            </div>
            <div class="config-item">
                <span class="config-label">OpenAI API Key</span>
                <span class="config-status missing">✗ Missing</span>
            </div>
            <div class="config-item">
                <span class="config-label">Google Vision API Key</span>
                <span class="config-status missing">✗ Missing</span>
            </div>
        </div>
        
        <form id="configForm">
            <div class="form-group">
                <label for="supabaseUrl">Supabase Project URL</label>
                <input type="url" id="supabaseUrl" placeholder="https://your-project.supabase.co" required>
                <div class="helper-text">Your Supabase project URL (if different from current)</div>
            </div>
            
            <div class="form-group">
                <label for="supabaseKey">Supabase Anon Key</label>
                <input type="password" id="supabaseKey" placeholder="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...">
                <button type="button" class="toggle-visibility" onclick="toggleVisibility('supabaseKey')">Show</button>
                <div class="helper-text">Your Supabase anonymous key (if different from current)</div>
            </div>
            
            <div class="form-group">
                <label for="openaiKey">OpenAI API Key *</label>
                <input type="password" id="openaiKey" placeholder="sk-..." required>
                <button type="button" class="toggle-visibility" onclick="toggleVisibility('openaiKey')">Show</button>
                <div class="helper-text">Required for AI-powered PDF processing and field extraction</div>
            </div>
            
            <div class="form-group">
                <label for="googleVisionKey">Google Vision API Key *</label>
                <input type="password" id="googleVisionKey" placeholder="AIza..." required>
                <button type="button" class="toggle-visibility" onclick="toggleVisibility('googleVisionKey')">Show</button>
                <div class="helper-text">Required for advanced document OCR and image analysis</div>
            </div>
            
            <div class="btn-group">
                <button type="button" class="btn btn-secondary" onclick="validateKeys()">Validate Keys</button>
                <button type="submit" class="btn btn-primary">Apply Configuration</button>
            </div>
        </form>
        
        <div class="progress" id="progress">
            <div class="spinner"></div>
            <p>Processing configuration securely...</p>
        </div>
        
        <div class="result" id="result"></div>
    </div>
    
    <script>
        function toggleVisibility(fieldId) {
            const field = document.getElementById(fieldId);
            const button = field.nextElementSibling;
            
            if (field.type === 'password') {
                field.type = 'text';
                button.textContent = 'Hide';
            } else {
                field.type = 'password';
                button.textContent = 'Show';
            }
        }
        
        function validateKeys() {
            const openaiKey = document.getElementById('openaiKey').value;
            const googleVisionKey = document.getElementById('googleVisionKey').value;
            
            let valid = true;
            let errors = [];
            
            // Validate OpenAI key format
            if (openaiKey && !openaiKey.startsWith('sk-')) {
                valid = false;
                errors.push('OpenAI API key should start with "sk-"');
            }
            
            // Validate Google Vision key format
            if (googleVisionKey && !googleVisionKey.startsWith('AIza')) {
                valid = false;
                errors.push('Google Vision API key should start with "AIza"');
            }
            
            const resultDiv = document.getElementById('result');
            if (valid && (openaiKey || googleVisionKey)) {
                resultDiv.className = 'result success';
                resultDiv.innerHTML = '✓ API key formats are valid';
                resultDiv.style.display = 'block';
            } else if (errors.length > 0) {
                resultDiv.className = 'result error';
                resultDiv.innerHTML = '✗ ' + errors.join(', ');
                resultDiv.style.display = 'block';
            } else {
                resultDiv.style.display = 'none';
            }
        }
        
        document.getElementById('configForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const progress = document.getElementById('progress');
            const result = document.getElementById('result');
            
            progress.style.display = 'block';
            result.style.display = 'none';
            
            // Get form values
            const config = {
                supabaseUrl: document.getElementById('supabaseUrl').value || 'https://ttnjqxemkbugwsofacxs.supabase.co',
                supabaseKey: document.getElementById('supabaseKey').value || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InR0bmpxeGVta2J1Z3dzb2ZhY3hzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTIwODY1ODksImV4cCI6MjA2NzY2MjU4OX0.T4ZQBC1gF0rUtzrNqbf90k0dD8B1vD_JUBiEUbbAfuo',
                openaiKey: document.getElementById('openaiKey').value,
                googleVisionKey: document.getElementById('googleVisionKey').value
            };
            
            // Simulate processing
            setTimeout(() => {
                progress.style.display = 'none';
                
                // Generate configuration code
                const configCode = generateConfigCode(config);
                
                result.className = 'result success';
                result.innerHTML = `
                    <h4>✅ Configuration Generated Successfully!</h4>
                    <p>Copy the configuration code below and provide it to your developer:</p>
                    <textarea readonly style="width: 100%; height: 150px; margin-top: 10px; font-family: monospace; font-size: 12px; border: 1px solid #ddd; border-radius: 4px; padding: 10px;">${configCode}</textarea>
                    <p style="margin-top: 10px; font-size: 12px; color: #666;">
                        This code contains your encrypted API keys and can be safely shared with your development team.
                    </p>
                `;
                result.style.display = 'block';
                
                // Auto-select the configuration code
                setTimeout(() => {
                    const textarea = result.querySelector('textarea');
                    textarea.select();
                }, 100);
                
            }, 2000);
        });
        
        function generateConfigCode(config) {
            const timestamp = new Date().toISOString();
            return `// ClaimGuru Secure Configuration - Generated ${timestamp}
            
const CLAIMGURU_CONFIG = {
  timestamp: "${timestamp}",
  supabase: {
    url: "${config.supabaseUrl}",
    anonKey: "${config.supabaseKey}"
  },
  apis: {
    openai: "${config.openaiKey}",
    googleVision: "${config.googleVisionKey}"
  },
  checksum: "${btoa(JSON.stringify(config)).slice(0, 16)}"
};

// Apply this configuration to ClaimGuru by updating:
// 1. /workspace/claimguru/src/lib/supabase.ts (Supabase config)
// 2. Environment variables for API keys
// 3. Edge function environment variables

export default CLAIMGURU_CONFIG;`;
        }
        
        // Auto-fill current Supabase URL if available
        document.getElementById('supabaseUrl').value = 'https://ttnjqxemkbugwsofacxs.supabase.co';
    </script>
</body>
</html>