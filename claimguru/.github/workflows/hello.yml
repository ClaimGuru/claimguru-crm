name: Hello World

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./claimguru
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from GitHub Actions in claimguru directory!"
      - run: ls -la pnpm-lock.yaml
